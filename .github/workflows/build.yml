# name: StreamLens CI/CD Pipeline

# on:
#   push:
#     # branches: [ main, master ]
#   # pull_request:
#   #   branches: [ main, master ]

# jobs:
#   test:
#     name: Lint & Headless Testing
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Set up Python 3.11
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       - name: Install System Dependencies (OpenCV & Qt support)
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y libgl1-mesa-glx libglib2.0-0 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-util1 libxcb-xfixes0 libxcb-xkb1 libxkbcommon-x11-0 libdbus-1-3 xvfb

#       - name: Install Python Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install flake8 pytest

#       - name: Run Flake8 Linter
#         run: |
#           # stop the build if there are Python syntax errors or undefined names
#           flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
#           # exit-zero treats all errors as warnings.
#           flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

#       - name: Run Unit Tests (Headless Qt)
#         env:
#           QT_QPA_PLATFORM: offscreen
#         run: |
#           python -m unittest discover -s tests

#   build-windows:
#     name: Package Windows Executable
#     needs: test
#     if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
#     runs-on: windows-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Set up Python 3.11
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       - name: Install Python Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Build Executable with PyInstaller
#         run: |
#           # Build standard single-file GUI executable
#           pyinstaller --noconfirm --onedir --windowed --add-data "src;src" --name "StreamLens" src/main.py

#       - name: Archive Production Artifacts
#         uses: actions/upload-artifact@v3
#         with:
#           name: StreamLens-Windows-Build
#           path: dist/StreamLens/
